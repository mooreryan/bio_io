license: ["MIT" "Apache-2.0"]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test & arch != "x86_32"}
    "@doc" {with-doc}
  ]
]
